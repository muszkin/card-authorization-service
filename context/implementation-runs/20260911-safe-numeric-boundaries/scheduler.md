# Scheduler

S1 -> S2 -> final gates -> PR/CI -> merge/main CI. Shared acceptance files require serialization.
No parallel runtime gates. Independent review may overlap documentation-only coordination.
