# Resources

Diagnostic runtime RELEASED. S1 runtime REQUESTED.
One serialized runtime lease; ephemeral WireMock/application ports, per-process Testcontainers Postgres/Redis.
Worktree-local build outputs; Gradle shared cache uses its native locks. No Compose or shared services.
Worker must retain actual port/container evidence and verify teardown before release.
