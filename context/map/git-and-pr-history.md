<!-- BEGIN project-context-initializer:artifact -->
# Git and PR history

Generated by project-context-initializer from `b4bef16` (main, clean worktree) on 2026-09-07T14:57:44Z. Navigation evidence only: current user instructions, code, tests, runtime behaviour and canonical docs outrank this file.

## Preflight (Observed)

- Branch `main`, HEAD `b4bef1631aa5cfb1efc1655b06e36f38c788f40a`; full history, not shallow; 36 commits; one author (Piotr Mucha), no bots, no merges.
- Window: authored 2026-09-07T11:21:08Z to 2026-09-07T12:21:21Z. HEAD is an amended replacement of a
  previously pushed commit (`d34d97e`), committed later the same day; see risks.
- Filters: none needed (no vendor, lock, generated or mass-format commits). Largest commit: HEAD, 12 files (docs).
- PR provider: GitHub via authenticated read-only `gh` (`gh pr list --state all --limit 20`): 0 pull requests.
  All work landed directly on `main`, which is the documented convention (DECISIONS.md §16).
- CI signal: `gh run list --branch main` shows every run since the workflow was added succeeded
  (`fae7f76`, `7dcffd2`, `14ed9e1`, `d34d97e`, `b4bef16`).

## Hotspots (commits touching a file; Observed)

| File | Commits | Reading |
| --- | --- | --- |
| `src/main/resources/application.yaml` | 9 | every feature added its properties |
| `build.gradle.kts` | 8 | dependencies and test suites grew per step |
| `domain/authorization/Authorization.java` | 4 | decision -> idempotency -> transitions |
| `adapter/out/persistence/JdbcAuthorizationRepository.java` | 4 | schema-backed queries added per feature |
| `testFixtures/.../InMemoryAuthorizationRepository.java` | 4 | fake grows with the port |
| `integrationTest/.../AuthorizationRepositoryTest.java` | 4 | one spec per repository capability |

## Co-change (per commit; Observed)

Port <-> JDBC implementation <-> integration spec <-> in-memory fake move together (3 commits each pair), as do
`CardAuthorizationApplication.java` <-> `application.yaml` <-> `build.gradle.kts`. This is the expected shape
of the test-first sequence, not a coupling smell: a `test:` commit adds the spec and fake, the next `feat:`
commit adds the port method and adapter.

## Direction of recent work

Commits 1-31 build the service in red/green pairs; 32 adds CI; 33-36 add documentation and agent context.

## Caveats

One day, one author: hotspots describe the build order, not long-term churn; contributor signals identify a
single contact. Historical practice (direct commits to `main`, no PRs) is documented policy for this repository
and not a recommendation for others.
<!-- END project-context-initializer:artifact -->
