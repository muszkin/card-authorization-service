<!-- BEGIN project-context-initializer:artifact -->
# Risks and unknowns

Generated by project-context-initializer from `b4bef16` (main, clean worktree) on 2026-09-07T14:57:44Z. Navigation evidence only: current user instructions, code, tests, runtime behaviour and canonical docs outrank this file.

| # | Item | Provenance | Decision state | Impact | Next action | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `DECISIONS.md` §16 at `b4bef16` stated that no commit was amended, while `b4bef16` itself is an amended replacement of pushed commit `d34d97e` (GitHub Actions run 34121427075 references the old SHA). | Observed | Decided | none after the fix | resolved in the commit that adds this map: §16 now says the delivery history was never amended and the final documentation commit was amended once before review | repository author |
| 2 | Every authorization reads the card's full ledger (`JdbcLedgerRepository.entriesFor`). | Observed | Accepted risk (README, DECISIONS §2) | latency grows with ledger size | snapshot entries + delta when needed | repository author |
| 3 | Expiry sweep processes a batch in one transaction; one authorization that cannot be expired fails the batch until the next minute. | Observed | Accepted risk (README) | delayed releases under a rare race | per-authorization transaction or skip-on-conflict | repository author |
| 4 | `IllegalArgumentException` is mapped to 400 globally; a programming error of that type would surface as a client error. | Observed | Accepted risk (DECISIONS §14) | misleading status in edge cases | dedicated domain exception type if the API grows | repository author |
| 5 | Advisory lock key is `hashtext(cardId)` (32-bit): collisions over-serialize unrelated cards. | Observed | Accepted risk (DECISIONS §4) | throughput only, never correctness | 64-bit key if contention appears | repository author |
| 6 | Production topology, message broker, API gateway and authentication are not defined. | Unknown | Out of scope (brief) | none for the deliverable; blocks any deployment | decide when productizing | repository author |
| 7 | Real risk engine contract is known only through the WireMock stub. | Unknown | N/A | integration risk | contract test against the real service | repository author |
| 8 | Codex discoverability could not be smoke-tested (`codex` CLI absent); Claude discoverability not smoke-tested in a fresh session. | Observed | N/A | routers unverified in fresh sessions | open a fresh session in the repository root and confirm `context/map/INDEX.md` is loaded | repository author |
| 9 | Size: 1447 non-blank production lines against a brief that asks for "small". | Observed | Accepted risk (README) | reviewer perception | none | repository author |

No stale evidence: every document was read at `b4bef16` with a clean worktree. No unreadable sources.
<!-- END project-context-initializer:artifact -->
